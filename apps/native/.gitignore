# Generated by xcodegen from project.yml — never hand-edited, never committed.
Leeft.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
.swiftpm/
Package.resolved

# Firebase iOS config — download per-machine from the console (see README).
# Carries the OAuth client id, so it stays out of git like apps/web/.env.local does.
Resources/GoogleService-Info.plist
